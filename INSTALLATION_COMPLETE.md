# ✅ Повне автоматичне встановлення MIMIC на VPS

## 🚀 Один скрипт - все встановлюється автоматично!

Тепер достатньо запустити **один файл** і все встановиться автоматично:

```bash
# Клонувати репозиторій
git clone https://github.com/mimiccash-ops/MIMIC.git
cd MIMIC

# Запустити встановлення
sudo chmod +x install_vps.sh
sudo ./install_vps.sh
```

## ✨ Що робить скрипт автоматично:

1. ✅ Оновлює систему та встановлює всі залежності
2. ✅ Створює користувача `mimic`
3. ✅ Клонує репозиторій з GitHub
4. ✅ Створює Python virtual environment
5. ✅ Встановлює всі Python пакети
6. ✅ Збирає фронтенд (Tailwind CSS)
7. ✅ Встановлює та налаштовує PostgreSQL
8. ✅ Встановлює та налаштовує Redis
9. ✅ Створює конфігураційні файли (.env, config.ini)
10. ✅ **Виправляє Sentry DSN** (видаляє невалідні значення)
11. ✅ **Виправляє DATABASE_URL** (localhost замість Docker імен)
12. ✅ **Виправляє REDIS_URL** (localhost замість Docker імен)
13. ✅ **Встановлює правильний пароль PostgreSQL**
14. ✅ **Запускає PostgreSQL та Redis**
15. ✅ Запускає міграції бази даних
16. ✅ Створює systemd сервіси
17. ✅ Налаштовує firewall
18. ✅ Встановлює Nginx (опціонально)

## 📋 Після встановлення:

### 1. Налаштувати API ключі:

```bash
sudo nano /var/www/mimic/config.ini
```

Додайте:
- Binance API ключі в `[MasterAccount]`
- Telegram bot token в `[Telegram]`
- Webhook passphrase в `[Webhook]`

### 2. Запустити сервіси:

```bash
sudo systemctl start mimic
sudo systemctl start mimic-worker
sudo systemctl start mimic-bot
```

### 3. Перевірити статус:

```bash
sudo systemctl status mimic
```

## 🔧 Опції скрипта:

```bash
# Пропустити Nginx
sudo ./install_vps.sh --skip-nginx

# Пропустити PostgreSQL (якщо вже встановлено)
sudo ./install_vps.sh --skip-db

# Пропустити Redis (якщо вже встановлено)
sudo ./install_vps.sh --skip-redis

# Встановити в іншу директорію
sudo ./install_vps.sh /opt/mimic
```

## 📊 Важливі дані (збережіть):

Після встановлення скрипт покаже:
- **Database User:** `mimic_user`
- **Database Name:** `mimic_db`
- **Database Password:** (буде показано під час встановлення)

## 🎯 Наступні кроки:

1. Додати API ключі в `config.ini`
2. Запустити сервіси
3. Налаштувати Nginx (див. `LINUX_DEPLOYMENT.md`)
4. Налаштувати SSL сертифікат

## 📚 Документація:

- `LINUX_DEPLOYMENT.md` - Детальна інструкція з налаштування
- `POST_INSTALL_FIXES.md` - Виправлення проблем після встановлення
- `GIT_UPDATE_COMMANDS.md` - Команди для оновлення з GitHub

---

**Готово!** Тепер встановлення займає одну команду! 🎉
