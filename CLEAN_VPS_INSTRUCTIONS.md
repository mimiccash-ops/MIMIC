# 🧹 Інструкція: Повне очищення VPS до базового стану

## ⚠️ УВАГА

Цей скрипт **ПОВНІСТЮ ВИДАЛЯЄ** всі файли та сервіси MIMIC з вашого VPS Linux.

**Що буде видалено:**
- ✅ Всі файли додатку MIMIC (`/var/www/mimic`)
- ✅ Всі systemd сервіси (mimic, mimic-worker, mimic-bot)
- ✅ Всі конфігурації Nginx для MIMIC
- ✅ Всі SSL сертифікати (Let's Encrypt)
- ✅ Всі бази даних (PostgreSQL, MySQL, SQLite)
- ✅ Всі Docker контейнери та volumes
- ✅ Всі дані Redis
- ✅ Користувач `mimic` та його домашня директорія
- ✅ Всі файли в `/root` (крім `.bashrc`, `.profile`, `.ssh`)

**Що залишиться:**
- ✅ Базова Linux система
- ✅ SSH доступ
- ✅ Системні пакети
- ✅ Інші додатки (якщо є)

---

## 🚀 Як використовувати

### Варіант 1: Завантажити на VPS через SCP

```powershell
# З Windows PowerShell
cd "C:\Users\MIMIC Admin\Desktop\MIMIC v 4.0"
scp clean_vps.sh root@YOUR_VPS_IP:/root/
```

### Варіант 2: Створити файл безпосередньо на VPS

```bash
# Підключитися до VPS
ssh root@YOUR_VPS_IP

# Створити файл
nano /root/clean_vps.sh
# (вставити вміст clean_vps.sh)

# Зробити виконуваним
chmod +x /root/clean_vps.sh
```

---

## ▶️ Запуск скрипта

```bash
# Підключитися до VPS
ssh root@YOUR_VPS_IP

# Запустити скрипт
cd /root
./clean_vps.sh
```

**Скрипт запитає підтвердження:**
- Введіть `YES` (великими літерами) для підтвердження
- Скрипт почне видаляти все поетапно
- В кінці запитає, чи видаляти Python пакети (опціонально)

---

## 📋 Що робить скрипт (поетапно)

1. **Зупиняє всі сервіси** - mimic, mimic-worker, mimic-bot
2. **Видаляє systemd сервіси** - файли з `/etc/systemd/system/`
3. **Видаляє конфіги Nginx** - sites-available/enabled
4. **Видаляє SSL сертифікати** - через certbot delete
5. **Видаляє файли додатку** - `/var/www/mimic` та альтернативні локації
6. **Видаляє Docker** - контейнери та volumes
7. **Видаляє бази даних** - PostgreSQL, MySQL, SQLite
8. **Очищає Redis** - FLUSHALL
9. **Видаляє користувача** - mimic user та home directory
10. **Очищає /root** - всі файли крім базових

---

## 🔧 Налаштування перед запуском

Якщо ваш проект знаходиться не в `/var/www/mimic`, або домен не `mimiccash.com`, відредагуйте змінні на початку скрипта:

```bash
APP_DIR="/var/www/mimic"      # Шлях до проекту
APP_USER="mimic"              # Користувач додатку
DOMAIN="mimiccash.com"        # Ваш домен
```

---

## ⚡ Швидкий запуск (одна команда)

```bash
# Завантажити та запустити одразу
curl -o /tmp/clean_vps.sh https://raw.githubusercontent.com/YOUR_REPO/main/clean_vps.sh 2>/dev/null || \
wget -O /tmp/clean_vps.sh https://raw.githubusercontent.com/YOUR_REPO/main/clean_vps.sh 2>/dev/null || \
echo "Завантажте файл вручну"

chmod +x /tmp/clean_vps.sh
/tmp/clean_vps.sh
```

---

## 🛡️ Безпека

- Скрипт **НЕ видаляє** базову систему
- Скрипт **НЕ видаляє** SSH доступ
- Скрипт **НЕ видаляє** системні пакети
- Скрипт **вимагає підтвердження** перед видаленням
- Скрипт **зберігає** `.bashrc`, `.profile`, `.ssh` в `/root`

---

## 🔄 Після очищення

Після успішного очищення ваш VPS буде в базовому стані. Ви зможете:

1. **Перевстановити MIMIC** - використовуючи `LINUX_DEPLOYMENT.md`
2. **Використати для іншого проекту** - VPS готовий до нового додатку
3. **Залишити як є** - чистий Linux сервер

---

## ❓ Troubleshooting

### Помилка: "Permission denied"
```bash
chmod +x clean_vps.sh
sudo ./clean_vps.sh
```

### Помилка: "Service not found"
Це нормально - означає, що сервіс вже видалений або не існував.

### Помилка: "User not found"
Це нормально - означає, що користувач вже видалений або не існував.

### Скрипт завис
Натисніть `Ctrl+C` і перевірте логи:
```bash
journalctl -xe
```

---

## 📞 Підтримка

Якщо щось пішло не так:
1. Перевірте логи: `journalctl -xe`
2. Перевірте статус сервісів: `systemctl status mimic*`
3. Перевірте процеси: `ps aux | grep mimic`
4. Перевірте файли: `ls -la /var/www/mimic`

---

**⚠️ ВАЖЛИВО:** Ця операція **НЕЗВОРОТНА**. Переконайтеся, що у вас є резервні копії важливих даних перед запуском!
